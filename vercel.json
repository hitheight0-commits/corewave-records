{
    "buildCommand": "npx prisma db push --accept-data-loss && node scripts/seed-production-content.js && npm run build",
    "devCommand": "npm run dev",
    "installCommand": "npm install",
    "framework": "nextjs",
    "outputDirectory": ".next",
    "build": {
        "env": {
            "SKIP_ENV_VALIDATION": "true"
        }
    }
}